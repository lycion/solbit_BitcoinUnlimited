#
# .gitattributes file
#
# NOTE: This file currently lists all files  that _might_ be required
# to be formatted properly. The files listed here are filtered once
# more by the list of files in .formatted-files before formatting
# is applied.

*.h filter=solbit-clang-format
*.cpp filter=solbit-clang-format
test/*.h filter=solbit-clang-format
test/*.cpp filter=solbit-clang-format
qt/*.h filter=solbit-clang-format
qt/*.cpp filter=solbit-clang-format